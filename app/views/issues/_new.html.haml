- if request.format == :html
  .card_header
    %h2 New Issue

.content{class: request.format == 'html' ? '' : 'invisible'}

  = render 'form'

  %br
  = link_to 'Back', issues_path, data: { remote: true, fade_content: true, swap_title: 'Issues' }
