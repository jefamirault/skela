<% if notice %>
  flashNotice('<%= notice %>');
<% else %>
  flashAlert('<%= alert %>');
<% end %>

<% if @user %>
hide('#user_form');
window.setTimeout(function(){
  $('tr[data-object-id=<%= @user.id %>]').addClass('invisible');
  window.setTimeout(function(){
//    $('tr[data-object-id=<%= @user.id %>]').remove();
//    $('users').DataTable().row($('tr[data-object-id=<%= @user.id %>]')).remove()
//    $('users').DataTable().draw();
  }, 450);
}, 200);
<% end %>
