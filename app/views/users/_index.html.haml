- if request.format == :html
  .card_header
    %h2 Users

.content{class: request.format == 'html' ? '' : 'invisible'}

  - if @users
    .center
      = megatable 'User', %w(id username favorite_color)

  -#%br
  -#.center
  -#  = link_to 'New User', new_user_path, remote: true, data: { fade_content: true, swap_title: 'New User'}

