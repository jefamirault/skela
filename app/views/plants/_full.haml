.subject_container
  = ajax_subject 'name', @resource

.notes_container
  = ajax_notes 'notes', @resource

= edit_fields @resource, species: :string,
                         birthday: :datetime


%footer
  = destroy_link @resource, false
