- if request.format == :html
  .card_header(style="width: 592px; margin: 0 auto;")
    %h2 Editing Task

.content{class: request.format == 'html' ? '' : 'invisible'}
  = render 'form'

  %br
  .center
    = link_to 'Back', tasks_path, remote: true, data: { fade_content: true, swap_title: 'Tasks'}
