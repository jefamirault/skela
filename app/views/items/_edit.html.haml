- if request.format == :html
  .card_header
    %h2 Editing Item

.content{class: request.format == 'html' ? '' : 'invisible'}
  = render 'form'

  %br
  .center
    = link_to 'Back', items_path, remote: true, data: { fade_content: true, swap_title: 'Items'}
