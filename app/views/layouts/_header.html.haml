#header
  %nav
    = nav_link 'Courses', courses_path, 'Courses', page_title: true

    %ul#primary_nav
      = render partial: 'layouts/primary_nav'

    .flex
    = my_profile_nav do
      - present current_user do |user|
        = user.username_and_avatar
