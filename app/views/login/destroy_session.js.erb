setTimeout(function(){
  $('#container').html('<%= j render partial: 'login/new_session' %>');
  $('#container').removeClass('hidden');
  decloak('.content')
  $('#username').focus();
  flashNotice("Logged out successfully!");
  window.history.replaceState( {} , '', '<%= login_path %>' );
}, 800);
