var crud = find_crud('<%= @resource.class.to_s.underscore %>',<%= @resource.id %>);
crud.find('.compact').html('<%= j render 'compact', resource: @resource %>');

var other = locate('<%= @resource.class.to_s.underscore %>',<%= @resource.id %>);
other.find('span[data-field="title"]').html('<%= @resource.title %>');
