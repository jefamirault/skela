#by_category
  #assignments_container.category
    .row{ style: 'align-items: flex-end;'}
      %h4 Assignments
      - if allow_create
        .new_crud_button{ style: 'display:inline;'}
          = link_to md_icon('add_circle', false), new_assignment_path, remote: true, class: 'new_crud'
    = cruddier_table @assignments, id: 'assignments'
  #exams_container.category
    .row{ style: 'align-items: flex-end;'}
      %h4 Exams
      - if allow_create
        .new_crud_button
          = link_to md_icon('add_circle', false), new_exam_path, remote: true, class: 'new_crud'
    = cruddier_table @exams, id: 'exams'
  #readings_container.category
    .row{ style: 'align-items: flex-end;'}
      %h4 Readings
      - if allow_create
        .new_crud_button
          = link_to md_icon('add_circle', false), new_reading_path, remote: true, class: 'new_crud'
    = cruddier_table @readings, id: 'readings'
  #resources_container.category
    .row{ style: 'align-items: flex-end;'}
      %h4 Resources
      - if allow_create
        .new_crud_button
          = link_to md_icon('add_circle', false), new_resource_path, remote: true, class: 'new_crud'
    = cruddier_table @resources, id: 'resources'
